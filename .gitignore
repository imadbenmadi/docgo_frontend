node_modules
dist
dist-ssr
*.local

logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# .env and .env.development are committed on purpose: they hold public
# build-time values only, and the GitHub Actions build reads them from the
# checkout. Ignore them and the deployed bundle is built with VITE_API_URL
# undefined.
